plugins {
    id 'java'
    id 'pmd'
}

repositories {
    jcenter()
}

pmd {
    consoleOutput = true
    toolVersion = "6.0.0"
    rulesMinimumPriority = 5
    ruleSets = ["/rulesets/java/braces.xml", "/rulesets/java/naming.xml"]
}

dependencies {
}
