plugins {
    id 'java'
    id 'pmd'
}

repositories {
    jcenter()
}

apply plugin: 'pmd'
pmd {
    consoleOutput = true
    toolVersion = "6.0.0"
    rulesMinimumPriority = 5
    ruleSets = ["category/java/errorprone.xml", "category/java/bestpractices.xml", "/rulesets/java/naming.xml"]
}

dependencies {
}
